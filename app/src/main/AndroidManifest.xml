<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.febi.mfkids">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/home_title"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".AddKidActivity"
            android:label="@string/title_activity_add_kid"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".KidMileStonesActivity"
            android:label="@string/kid_milestones_title"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".AddMileStoneActivity"
            android:label="@string/add_milestone_title"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>